# 作品集项目使用与部署说明

## 1. 项目简介

这是一个无需构建工具的纯静态作品集网站，可直接部署到 GitHub Pages、任意静态网站托管平台或普通 Web 服务器。

项目入口与主要文件：

- `index.html`：网站页面结构与入口。
- `styles.css`：页面样式、响应式布局和动画样式。
- `portfolio.js`：交互逻辑和作品数据。
- `resources/`：图片、视频、SVG 等静态资源。
- `.nojekyll`：让 GitHub Pages 跳过 Jekyll 处理。
- `start.bat`：Windows 本地一键预览。
- `static-server.js`：没有 Python 时使用的 Node.js 静态服务器。
- `deploy.bat`：一键提交并推送到 GitHub。

## 2. 本地运行

### 一键启动（推荐）

双击根目录中的 `start.bat`。脚本将：

1. 进入项目根目录；
2. 优先使用 Python，找不到 Python 时使用 Node.js；
3. 启动本地服务器；
4. 自动打开 `http://127.0.0.1:8000/`。

关闭脚本窗口或按 `Ctrl+C` 即可停止服务。

如果提示 8000 端口已被占用，可关闭占用该端口的软件，或同时修改 `start.bat` 中的 `PORT` 和访问地址。

### 环境要求

满足以下任意一项即可：

- Python 3；
- Node.js 16 或更高版本。

项目没有 npm 依赖，无需执行 `npm install`。

## 3. 修改网站

编辑文件后刷新浏览器即可查看变化。脚本设置了 `no-cache`，通常无需清理缓存；若页面仍未更新，可按 `Ctrl+F5` 强制刷新。

添加资源时建议：

- 将文件放入 `resources/` 或其子目录；
- 文件名尽量使用英文、数字和连字符；
- HTML、CSS 和 JavaScript 中使用相对路径，例如 `resources/demo.png`；
- 视频建议使用浏览器兼容性较好的 H.264 编码 MP4；
- 发布前检查文件大小，避免超大图片和视频拖慢加载速度。

## 4. GitHub Pages 一键部署

### 首次配置

当前仓库默认远程地址为：

`https://github.com/Toke-kang/portfolio.git`

当前默认分支为 `main`。在 GitHub 仓库中进入：

`Settings` → `Pages` → `Build and deployment`

选择：

- Source：`Deploy from a branch`
- Branch：`main`
- Folder：`/ (root)`

保存后等待 GitHub Pages 完成首次发布。

### 日常一键部署

双击 `deploy.bat`。脚本会显示即将发布的变更，并在确认后执行：

1. `git add -A` 暂存全部变更；
2. 创建 Git 提交；
3. 使用 rebase 同步远程分支；
4. 推送当前分支到 GitHub。

注意：脚本会发布项目目录中的全部新增、修改和删除。确认前请仔细查看脚本显示的文件列表。

首次推送时，Git 可能要求通过浏览器登录 GitHub。若仓库使用 HTTPS，也可以使用 Git Credential Manager 或 Personal Access Token 完成认证。

## 5. 手动部署方式

如果不使用脚本，可在项目根目录执行：

```powershell
git status
git add -A
git commit -m "Update portfolio"
git pull --rebase origin main
git push origin main
```

也可以将以下内容上传到其他静态托管平台：

- `index.html`
- `styles.css`
- `portfolio.js`
- `resources/`
- `.nojekyll`（仅 GitHub Pages 必需）

站点没有服务端接口、数据库或环境变量。

## 6. 发布前检查清单

- 使用 `start.bat` 完整浏览首页和作品详情；
- 检查桌面端与移动端布局；
- 确认所有图片、视频和 SVG 均能加载；
- 检查浏览器开发者工具中是否存在 404 或 JavaScript 报错；
- 确认个人资料、联系方式和作品文案正确；
- 使用 `git status` 确认没有误加入隐私文件或临时文件；
- 执行 `deploy.bat`，等待 Pages 构建完成后再检查线上页面。

## 7. 常见问题

### 双击启动后一闪而过

通常是没有安装 Python 或 Node.js。打开命令提示符，执行 `python --version` 或 `node --version` 检查环境。

### 页面可以打开，但图片或视频显示 404

检查引用路径、文件名大小写和扩展名。GitHub Pages 运行在 Linux 环境，文件名大小写必须完全一致。

### 推送被拒绝

可能是远程仓库存在新提交、登录失效或没有写入权限。先执行 `git status` 查看状态。若有 rebase 冲突，应手动解决冲突后执行 `git rebase --continue`，再重新运行部署脚本。

### 已推送但线上没有更新

进入 GitHub 仓库的 `Actions` 或 `Settings → Pages` 查看发布状态。发布通常需要数十秒到几分钟，也可用 `Ctrl+F5` 排除浏览器缓存。

