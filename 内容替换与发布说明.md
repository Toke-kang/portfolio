# 网站内容替换与发布说明

本文用于日常替换作品图片、视频、文字并重新发布网站。

## 1. 修改前备份与检查

在项目根目录执行：

```powershell
git status
git pull --rebase origin main
```

若 `git status` 显示已有未提交修改，应先确认这些修改的来源，不要直接覆盖。重要资源可先复制一份到项目目录之外。

## 2. 替换图片或视频

最稳妥的方法是保持原文件名和扩展名不变：

1. 找到 `resources/` 中要替换的原文件；
2. 用新文件覆盖它；
3. 确认新文件类型与原扩展名一致；
4. 双击 `start.bat` 检查显示效果。

如果需要改文件名，必须同步修改 `index.html`、`styles.css` 或 `portfolio.js` 中引用该资源的路径。可在编辑器中全局搜索旧文件名，确保没有遗漏。

不要仅通过修改扩展名转换图片或视频格式。应使用图像或视频软件正确导出目标格式。

## 3. 修改作品标题、描述和页面文字

- 页面固定文字通常位于 `index.html`；
- 作品数据和交互文字通常位于 `portfolio.js`；
- 字体、颜色、间距、尺寸和响应式规则位于 `styles.css`。

修改 JavaScript 字符串时注意保留引号、逗号和括号。修改后打开浏览器开发者工具，确认 Console 中没有语法错误。

## 4. 新增作品资源

1. 将资源复制到 `resources/` 下合适的目录；
2. 在页面或作品数据中添加对应的相对路径；
3. 检查封面、详情、自动播放、静音和移动端效果；
4. 确认文件名没有容易混淆的空格或特殊符号；
5. 在本地预览中逐项点击验证。

推荐命名示例：

```text
resources/projects/project-name-cover.webp
resources/projects/project-name-detail-01.webp
resources/projects/project-name-demo.mp4
```

## 5. 删除旧资源

先全局搜索文件名，确认 HTML、CSS 和 JavaScript 已不再引用，再删除文件。发布后若出现 404，优先检查是否误删了仍在使用的资源。

## 6. 本地验收

双击 `start.bat`，访问 `http://127.0.0.1:8000/`，至少检查：

- 首页首次加载；
- 各作品入口及详情；
- 图片比例和清晰度；
- 视频播放、循环和静音状态；
- 导航、按钮、返回与弹窗交互；
- 手机窄屏和桌面宽屏布局；
- 浏览器 Network 面板没有 404；
- Console 面板没有红色错误。

## 7. 正式发布

双击 `deploy.bat`，查看变更列表后输入 `y`，再填写本次发布说明，例如：

```text
Replace vivo project images and update descriptions
```

脚本完成后，到 GitHub 仓库确认最新提交和 Pages 状态。线上页面更新后再做一次完整检查。

## 8. 发布回退

若线上内容有误，不要删除 `.git` 或强制重置仓库。推荐使用 GitHub 网站对错误提交执行 `Revert`，生成一个可追踪的反向提交；也可以修正文件后再次运行 `deploy.bat` 发布。

如需精确回退但不熟悉 Git，请先保留当前文件并寻求协助，避免使用 `git reset --hard` 或强制推送。

## 9. 发布记录建议

每次提交信息应说明改了什么，避免使用“修改”“更新”等过于模糊的描述。推荐格式：

```text
Update homepage profile copy
Replace AI workstation project media
Fix mobile layout and video loading
```
